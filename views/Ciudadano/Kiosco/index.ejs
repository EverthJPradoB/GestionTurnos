<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Generador de Ticket</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        .tramite-card {
            cursor: pointer;
            transition: transform 0.2s, border 0.2s;
            border: 2px solid transparent;
        }

        .tramite-card:hover {
            transform: scale(1.05);
        }

        .selected {
            border-width: 4px !important;
            box-shadow: 0px 0px 10px rgba(0, 123, 255, 0.5);
        }

        #codigo_ticket {
            display: none;
            font-size: 2rem;
            font-weight: bold;
        }

        #generar_ticket_btn {
            display: none;
            transition: all 0.3s;
        }

        .logo {
            max-width: 100px;
            /* Ajusta según el tamaño del ticket */
            height: 100px;
            /* Mantiene la proporción */
            margin-bottom: 10px;
        }
    </style>
</head>

<body class="bg-light">
    <div class="container mt-5">
        <div class="card shadow-lg p-4">
            <div class="card-body text-center">
                <h1 class="card-title">Generador de Ticket</h1>
                <!-- <p>Seleccione el tipo de trámite:</p> -->
                <p></p>
                <!-- Opciones de trámite en tarjetas -->
                <div class="row pt-5" id="lista_tramites" >

                </div>

                <!-- Botón de generar ticket -->
                <button id="generar_ticket_btn" class="btn btn-primary btn-lg mt-3">Generar Ticket</button>

                <!-- Mensaje de carga -->
                <div id="loading" class="mt-3" style="display: none;">
                    <div class="spinner-border text-primary" role="status">
                        <span class="sr-only">Generando ticket...</span>
                    </div>
                </div>

                <!-- Mensaje de resultado -->
                <div id="mensaje_resultado" class="my-3"></div>

                <!-- Mostrar el código del ticket generado -->
                <!-- <div id="codigo_ticket" class="mt-4">
                    <p class="h4">Tu número de turno es:</p>
                    <p id="codigo_turno" class="h1 text-primary"></p>
                </div> -->


            </div>

        </div>
    </div>

    <div id="lista_tramites"></div>



    <%- include('../../html/MainJs') %>

    

    <script src="/js/custom/kiosko_ws.js"></script>

    <script src="/js/custom/kiosko.js"></script>

</body>

</html>